export interface IUserProfile {
    id: string;
    firstName: string;
    lastName: string;
    // email: string;
    role: string;
    avatarImgUrl?: string;
    // password: string;
    // confirmed: boolean;
    // isDiabled: boolean;
}